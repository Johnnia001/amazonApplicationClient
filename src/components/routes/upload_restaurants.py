# len(restaurants)
#597
# from tqdm import tqdm
# r = requests.get('http://localhost:8080/restaurant/getAll')
# loaded_rs = json.loads(r.text)
# loaded_rs

#for restaurant in tqdm(restaurants2):
#...     r = requests.post('http://localhost:8080/restaurant/add', #json=restaurant)
#... if r.status_code != 200:
#... print("Failed:", restaurant)
